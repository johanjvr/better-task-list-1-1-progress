        @using (Html.BeginForm("PostComment", "Tickets", new { area="Tickets", id = Model.TicketId }, FormMethod.Post))
        {		
                <p>@Html.TextArea("TicketComment", new { @style = "width:568px; border: 1px solid #CCCCCC; height: 65px;" })</p>
            
                <div style="text-align:right;">
                    <input id="SubmitComment" type="submit" value="Post Comment" /> or @Html.ActionLink("Cancel", "Index", "Home", new { area = "" }, null)
                </div>
                
        }     

